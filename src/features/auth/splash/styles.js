import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
    backgroundColor: '#fff',
  },
  image: {
    resizeMode: 'contain',
    alignItems: 'center',
    width: 120,
    height: 120,
    justifyContent: 'center',
    alignSelf: 'center',
    paddingBottom: 10,
    paddingTop: 5,
  },
});
